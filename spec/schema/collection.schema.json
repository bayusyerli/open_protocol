{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://spec.openprotocols.id/v0.1/collection.schema.json",
  "title": "Collection — berkas kumpulan entitas terkurasi",
  "description": "Wadah untuk kosakata yang ditinjau sebagai satu kesatuan. Setiap item tetap membawa lifecycle dan pemetaannya sendiri; wadah ini hanya memudahkan peninjauan oleh dewan redaksi. Item divalidasi terhadap skema entitasnya oleh pemeriksa, berdasarkan entity_type.",
  "type": "object",
  "properties": {
    "$schema": {
      "type": "string"
    },
    "collection": {
      "type": "object",
      "properties": {
        "entity_type": {
          "type": "string",
          "description": "Menentukan skema yang dipakai memeriksa tiap anggota: `<entity_type>.schema.json`. Namanya menyebut BENTUK anggotanya, bukan klaim bahwa tiap anggota entitas ber-`op:` id — `nama-lokal` misalnya berisi pemetaan, dan pemetaan tidak diberi id karena bukan hal yang ada di dunia.",
          "enum": [
            "commodity",
            "variety",
            "stage-scale",
            "substance",
            "product",
            "operation-type",
            "variable",
            "method",
            "pest",
            "region",
            "deviation-reason",
            "preparation",
            "target-site",
            "nama-lokal",
            "principal",
            "harga"
          ]
        },
        "label": {
          "$ref": "common.schema.json#/$defs/LangText"
        },
        "scope": {
          "$ref": "common.schema.json#/$defs/LangText"
        },
        "lifecycle": {
          "$ref": "common.schema.json#/$defs/Lifecycle"
        },
        "provenance": {
          "$ref": "common.schema.json#/$defs/Provenance"
        },
        "storage": {
          "type": "string",
          "enum": [
            "inline",
            "ndjson"
          ],
          "default": "inline",
          "description": "'ndjson' berarti entitasnya ada di berkas .ndjson bersebelahan, satu per baris. Dipakai untuk kumpulan besar supaya diff-nya tetap terbaca per rekaman."
        },
        "count": {
          "type": "integer",
          "minimum": 0,
          "description": "Jumlah entitas yang diharapkan. Diperiksa terhadap isi berkas."
        },
        "id_blocks": {
          "type": "array",
          "minItems": 1,
          "description": "Rentang nomor ID yang diklaim berkas ini. Wajib bila jenis entitasnya dipakai lebih dari satu berkas — inilah yang membuat penambahan paralel tidak bertabrakan.",
          "items": {
            "type": "object",
            "properties": {
              "from": {
                "type": "integer",
                "minimum": 1
              },
              "to": {
                "type": "integer",
                "minimum": 1
              }
            },
            "required": [
              "from",
              "to"
            ],
            "additionalProperties": false
          }
        }
      },
      "required": [
        "entity_type",
        "label",
        "lifecycle"
      ],
      "additionalProperties": false
    },
    "items": {
      "type": "array",
      "minItems": 1
    }
  },
  "required": [
    "collection"
  ],
  "additionalProperties": false,
  "allOf": [
    {
      "if": {
        "properties": {
          "collection": {
            "properties": {
              "storage": {
                "const": "ndjson"
              }
            },
            "required": [
              "storage"
            ]
          }
        },
        "required": [
          "collection"
        ]
      },
      "then": {
        "not": {
          "required": [
            "items"
          ]
        }
      },
      "else": {
        "required": [
          "items"
        ],
        "properties": {
          "items": {
            "minItems": 1
          }
        }
      }
    }
  ]
}
