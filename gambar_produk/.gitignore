# Berkas gambar tidak masuk git. Yang di-commit adalah catatan tentang gambar
# (manifes, alat, fixture), bukan gambarnya — lihat README.md bagian hak cipta.
mentah/
ternormalkan/

# Turunan dari spec/vocab/product/*.ndjson + pukpes_data/principal_alias.csv.
# Bangun ulang dengan `python3 merek.py`.
indeks-merek.json
